#include <stdio.h>
void main()
{
  int i, reverse=0 ;
  int rmdr;
  printf("Enter the number\n");
  scanf("%d", &i);
  while(i !=0)
   {
     rmdr= i%10;
     reverse= reverse*10 + rmdr;
     i= i/10;
   }
  printf("%d", reverse);
  
  return 0;
}
